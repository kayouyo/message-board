<!--<h1>Messages#index</h1>-->
<!--<p>Find me in app/views/messages/index.html.erb</p>-->

<div class="panel panel-danger">
  <div class="panel-heading">
    List of Miffy's Friends
  </div>
    
  <div class="panel-body">
    <% @messages.each do |message| %>
      <span class="label label-danger"><%= message.name %></span>
        <%= message.body %>
        (<%= link_to "edit" , edit_message_path(message) %>
         <%= link_to "destroy" , message , method: :delete, data: { confirm: 'Do you really want to delete friend of Miffy？' } %>
        )
      <br>
    <% end %>
  </div>
</div>

<% #部分テンプレートを読み込んでいる %>
<%= render 'form' %>



